#include <stdio.h>
#include <malloc.h>

int split(char string[])
{
    int i = 0;
    int nL = 0;
    int nSize = 0;
    while(string[i])
    {
        if(string[i] == ' ' && i > 0)
        {
            char *temparray = malloc((nSize + 1) * sizeof(char));
            if(temparray == NULL)
            {
                printf("Failed to allocate memory\n");
                return -1;
            }
            for(int j = 0; j <= nSize; j++)
            {
                temparray[j] = string[j];
            }
            temparray[nSize] = '\0'; // add null terminator
            printf("%s", temparray);
        }
        nSize++, i++;
    }
    return 0;
}

int main()
{
    char string[] = "hello lol";
    split(string);
    return 0;
}
